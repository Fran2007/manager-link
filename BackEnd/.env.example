# 1. Atlas → Database Access → create user (e.g. Franklin) → set password
# 2. Atlas → Network Access → allow your IP (or 0.0.0.0/0 for dev)
# 3. Atlas → Connect → Drivers → copy connection string and paste below
MONGODB_URI=mongodb+srv://USER:PASSWORD@linkmanager.knnvqxf.mongodb.net/linkmanager?authSource=admin&appName=LinkManager
TOKEN_SECRET=change-me
PORT=4000
FRONTEND_URL=http://localhost:5173
